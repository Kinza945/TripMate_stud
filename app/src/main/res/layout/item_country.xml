<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_s"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/cardMuted"
    app:cardCornerRadius="@dimen/card_corner"
    app:cardElevation="0dp"
    app:useCompatPadding="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/spacing_s">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/country_image_card"
            android:layout_width="@dimen/image_country_size"
            android:layout_height="@dimen/image_country_size"
            app:cardCornerRadius="@dimen/card_corner_small"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/surface">

            <ImageView
                android:id="@+id/country_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/country_names"
                android:scaleType="centerCrop" />
        </com.google.android.material.card.MaterialCardView>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_s"
            android:layout_toEndOf="@id/country_image_card"
            android:orientation="vertical"
            android:paddingEnd="@dimen/spacing_s">

            <TextView
                android:id="@+id/country_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="@style/Text.Title"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/country_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xs"
                android:maxLines="2"
                android:ellipsize="end"
                android:textAppearance="@style/Text.Body"
                android:textColor="@color/onSurfaceVariant" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/country_favorite"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true"
            android:background="@color/surface"
            android:contentDescription="@string/love_names"
            android:padding="6dp"
            android:src="@drawable/star_one"
            android:tint="@color/primary" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>
